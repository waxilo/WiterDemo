/** Application-wide configuration constants. */
export const API_BASE_URL = "https://api.sloan.dpdns.org";

/** Idle delay (ms) before an edit triggers an automatic save. */
export const AUTOSAVE_IDLE_MS = 3000;
